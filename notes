TODO:
-> Διάσπαση των weights tfidf
   σε όλα τα documents
   (τώρα είναι όλα μαζί)
   Σε κάθε εμφάνιση στα documents
   θα αποθηκεύεται ο αριθμός που
   έχει υπολογιστεί τώρα.

ii = {"WORD": number, ...


________________________
-> PRINT DICTIONARY KEY + VALUE:
    for key, value in inverted_index.items():
        print(key, value)

    >>> THEN {58, 66, 47, 23}

-> RUN THROUGH ALL KEY + VALUES OF word_dict:
   for i in range(1, 100):
        for key, value in word_dict[i].items():
            print(i, key, value)

   >>> 98 FATAL 0